# 前端E2E联调测试方案

说明：以下命令默认先进入仓库内的 `fullstack/` 目录执行。

## 1. 目标

本方案用于补充单元测试与 API 接口测试之外的真实联调验证，重点覆盖前端路由、登录鉴权、角色权限跳转与关键业务闭环加载能力。

## 2. 技术方案

- 测试框架：Playwright
- 前端运行方式：Vite 开发服务
- 后端运行方式：Django 本地服务
- 测试数据准备：通过 `bootstrap_system` 初始化默认角色账号，并在用例执行时通过后端接口创建联调用档案与实体位置

## 3. 当前覆盖场景

### 3.1 管理员登录回跳链路

- 未登录直接访问系统管理页
- 自动跳转到登录页
- 登录成功后自动回跳系统管理页
- 页面可正常加载用户管理标签
- 可正常退出登录并回到登录页

### 3.2 借阅人权限限制链路

- 借阅人登录后不显示系统管理菜单
- 借阅人直接访问系统管理页时自动回退到工作台

### 3.3 审计员报表访问链路

- 审计员未登录访问报表中心时自动跳转登录
- 登录成功后回跳报表中心
- 页面标题正常加载

### 3.4 借阅业务闭环链路

- 通过后端接口初始化可借阅档案和实体位置
- 借阅人发起借阅申请并查看详情状态
- 管理员完成审批通过与出库登记
- 借阅人提交归还附件
- 管理员完成归还确认与重新入库
- 借阅人从通知中心回跳到业务详情页并确认流程已闭环

### 3.5 销毁业务闭环链路

- 通过后端接口初始化已上架档案和实体位置
- 档案员通过前端页面发起销毁申请并查看详情状态
- 管理员完成销毁审批通过
- 档案员上传销毁附件并完成执行登记
- 档案员从通知中心回跳到销毁详情页并确认流程已闭环

## 4. 执行方式

```bash
./tests/e2e_tests/run_e2e_tests.sh
```

或：

```bash
./scripts/run_tests.sh e2e
```

如需执行完整测试链路：

```bash
./scripts/run_tests.sh full
```

## 5. 输出结果

执行后会生成：

- `fullstack/runtime/test_results/e2e_tests/01_playwright_install.log`
- `fullstack/runtime/test_results/e2e_tests/02_playwright_run.log`
- `fullstack/runtime/test_results/e2e_tests/report.json`
- `fullstack/runtime/test_results/e2e_tests/html-report/`

## 6. 说明

- E2E 联调测试会自动启动独立的前后端测试服务，不依赖当前手工启动的开发进程。
- 后端测试环境使用独立 SQLite 数据文件，避免污染常规开发数据库。
