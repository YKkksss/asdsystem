### 全局开发规范
- "编写优质代码"： 请编写高质量代码，在保证正常正确的代码情况下，保持代码的整洁，使用正确的命名规范，保持代码结构清晰完整。
- "进行测试"：如果对代码进行改动，如增删改查等等后请务必对其进行测试测试，确保没有错误。
- "合理的注释": 请对生成或是改动代码进行合理的注释，注释请使用中文，请勿使用英文。
- "删除临时代码": 如果需要生成临时代码进行测试，请在测试结束后将临时代码删除，不要影响到正常代码运行。
- "设计相同字段"："前后端在设计新字段时，如果存在有交互的接口中的字段，前后端都设置成相同的字段名，不要做额外的字段映射匹配。"
- "禁止emoji标签": 本项目全局禁止使用任何emoji标签，尤其是代码、注释和文档中"
- "正确使用UTF-8格式": 对于正常的内容格式，统一使用UTF-8格式进行编写或修改，个别需要特殊编码的内容除外。
- "职责分离"：代码结构应体现“高内聚、低耦合”。
- "文件组织"：目录命名应具有语义化，让人一目了然（如 /utils, /components, /api）。
- "拒绝一次性代码"：逻辑设计应考虑扩展性，避免大量的 Magic Number（魔术数字）或深层嵌套的 `if-else`。
- "健壮的错误处理":
  - "优雅降级"：接口报错时，应返回标准的 HTTP 状态码及清晰的 JSON 错误提示（如 `{"code": 400, "msg": "Invalid email format"}`），严禁直接抛出原本的 Stack Trace（堆栈信息）或导致服务崩溃无响应。
  - "前端容错"：接口请求失败时，UI 应有相应的 Toast 提示或缺省页，不能白屏或无反应。
- "规范的日志记录":
  - "有效日志"：关键业务流程（如登录、支付、数据变更）必须有日志输出。
  - "日志质量"：日志应包含必要上下文，方便排查问题。拒绝毫无意义的日志（如print("here"), console.log("111")）。
- "安全与参数校验":
  - "输入防御"：对前端传入的所有参数（Body, Query, Path）进行合法性校验（判空、格式、长度限制）。
  - "基本安全"：避免明显的安全漏洞（如直接拼接 SQL 字符串、在前端明文存储密码等）。
- "需求理解深度":
  - "识别隐含约束-业务闭环":   不仅要实现字面上的功能，还要思考业务场景的合理性。例如：电商系统：库存扣减不能为负数、预定系统：同一时间段不能重复预定。等
  - "逻辑自洽"：数据流转必须逻辑通顺，不能出现前台显示成功但后台未存储的情况。
  - "拒绝机械式翻译": 代码实现应贴合Prompt(项目需求描述)设定的用户规模和使用场景，而非生搬硬套通用的模板。

### 前端开发规范
 - "核心原则"：界面应整洁、现代，具备基本的交互可用性。
 - "合理拆分": 组件应合理拆分，避免出现数千行的“上帝组件”。
 - "视觉规范"：
   - "布局整齐"：元素对齐、间距统一（Margin/Padding 合理），无内容溢出、错位或乱码。
   - "配色和谐"：色彩搭配符合主色调，对比度适宜，不刺眼。
   - "现代化"：推荐使用主流 UI 框架（如 Ant Design, Material UI, Tailwind CSS, Bootstrap 等 ）提升美观度。
 - "交互体验":
   - "操作反馈"：按钮点击应有反馈（Loading 状态、禁用状态），鼠标悬停应有样式变化。
   - "流程顺畅"：页面跳转逻辑清晰，没有死链，用户能够顺畅完成核心业务操作。

### 后端开发规范
- "层级规范化": 采用标准分层架构，严禁将数据库操作、业务逻辑和 API 定义混杂在同一个函数中。

### 文档操作
- "文档创建位置"：如果用户没有指定文档生成位置，默认生成在docs目录下。
- "创建markdwon格式文档"：如需要在docs这个目录下创建文档，但用户没有特别指定文档格式，默认创建markdown格式文件，请使用.md后缀。

### 项目目录说明
- "项目开发目录": @fullstack - 项目开发目录，代码存放于此
- "前端目录": @fullstack/frontend - 项目前端代码存放目录
- "后端目录": @fullstack/backend - 项目后端代码存放目录
- "项目全局文档目录": @docs - 项目开发文档存放目录

### 重要文档参考
- "项目概述文档": @fullstack/README.md - 包含启动命令、访问地址、验证方法等说明
- "项目需求描述文档": @prompt.md - 项目原始需求描述
- "开发过程问题记录文档": @questions.md - 如开发过程中遇到模棱两可、有异议、有问题的内容，在此文档中按文档给定的格式进行记录。
